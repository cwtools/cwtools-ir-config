complex_enum[named_colors] = {
    path = "game/common/named_colors"

    name = {
        colors = {
            # TODO: Because the hsv/rgb { values } syntax is likely not supported yet, and even if it is by some "colour_field" thing that should likely include the enum in the first place
            enum_name = colour_field
        }
    }
}